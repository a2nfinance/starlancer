import { Table } from "antd"
import { useEffect } from "react"

export const CandidateList = () => {
    useEffect(() => {
        // show candidate
    }, [])
    return (
        <Table
        
        />
    )
}