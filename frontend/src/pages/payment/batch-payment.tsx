import { BatchForm } from "@/components/batch-payment/BatchForm";

export default function BatchPayment() {
    return (
        <div style={{ maxWidth: 600, margin: "auto" }}>
            <BatchForm />
        </div>
    )
}